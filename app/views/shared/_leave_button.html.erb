<%= simple_form_for(current_user.event_subscriptions.find_by(event_id: @event.id), html: { method: :delete }, remote: true) do |f| %>
  <div><%= hidden_field_tag :event_id, @event.id %></div>
  <%= f.submit "Eliminar", class: "btn btn-default btn-registro btn-delete" %>
<% end %>